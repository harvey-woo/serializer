import Serializer from '../serializer'
import Unserializer from '../unserializer'
import { expect } from 'chai'
describe('test', () => {
  it('test', () => {
    // class O {}
    // const f = (a: any) => a
    // const b = { a: 1, b: 3, f, y: undefined }
    // const c = { s: b, o: { s: b, w: 1 }, f, k: new O}
    // b.y = c
    // c.a = [b, 22, 3]
    // const serializer = new Serializer()
    // const unserializer = new Unserializer()
    // const newC = unserializer.unserialize(serializer.serialize(c), {
    //   classes: {
    //     O
    //   }
    // })
    // expect(newC.s === newC.o.s).to.equal(true)
    expect(1).to.equal(1)
  })
})